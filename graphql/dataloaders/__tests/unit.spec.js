const { initTest, query } = require('../../../helpers/tests');

initTest();

describe('Unit data loader', () => {
  it('Should only make one request for units', async (done) => {
    done();
  });
});
